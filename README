1. Create a file and call it .env

2. paste this with the right data between the quotes:

OPENAI_API_KEY=""
TWITCH_USERNAME=""
TWITCH_TOKEN = ""

3. open the terminal where the project is and run the tool with this command: 
    node .\RandomQuestion.js